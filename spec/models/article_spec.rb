require 'rails_helper'

RSpec.describe Article, type: :model do
  # context 'validations' do 
  #   it 'will create a article when all fields are correct' do
  #     article =  create(:article)
  #     expect(article.id).to eq(Article.last.id)
  #   end

  #   it 'will raise error when title is not present or blank' do
  #     article = create(:article, title: "")
  #     expect(article.id).to eq(nil)
  #     expect(article.errors[:title]).to eq(["can't be blank"])
  #   end
  # end
end
